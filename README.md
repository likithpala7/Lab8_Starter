# Lab 8 - Starter
1) Within a Github action that runs whenever the code is pushed.
2) No, end to end tests are usually ran on the entire program instead of a single function.
3) No, you would not use a unit test to test a messaging feature of an application since it interacts with other features of the app.
4) Yes, you would use a unit test to test the max message length since it tests only one part of the app.